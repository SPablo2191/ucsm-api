import { SubjectRegistrationMapper } from './subject-registration.mapper';

describe('SubjectRegistrationMapper', () => {
  it('should be defined', () => {
    expect(new SubjectRegistrationMapper()).toBeDefined();
  });
});

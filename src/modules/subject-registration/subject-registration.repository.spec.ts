import { SubjectRegistrationRepository } from './subject-registration.repository';

describe('SubjectRegistrationRepository', () => {
  it('should be defined', () => {
    expect(new SubjectRegistrationRepository()).toBeDefined();
  });
});

import { CommissionScheduleMapper } from './commission-schedule.mapper';

describe('CommissionScheduleMapper', () => {
  it('should be defined', () => {
    expect(new CommissionScheduleMapper()).toBeDefined();
  });
});

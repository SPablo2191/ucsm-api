import { StudentCommissionMapper } from './student-commission.mapper';

describe('StudentCommissionMapper', () => {
  it('should be defined', () => {
    expect(new StudentCommissionMapper()).toBeDefined();
  });
});

import { InstallmentRepository } from './installment.repository';

describe('InstallmentRepository', () => {
  it('should be defined', () => {
    expect(new InstallmentRepository()).toBeDefined();
  });
});

import { AcademicProgramRepository } from './academic-program.repository';

describe('AcademicProgramRepository', () => {
  it('should be defined', () => {
    expect(new AcademicProgramRepository()).toBeDefined();
  });
});

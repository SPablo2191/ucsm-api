import { AcademicProgramMapper } from './academic-program.mapper';

describe('AcademicProgramMapper', () => {
  it('should be defined', () => {
    expect(new AcademicProgramMapper()).toBeDefined();
  });
});

import { EnrollmentRepository } from './enrollment.repository';

describe('EnrollmentRepository', () => {
  it('should be defined', () => {
    expect(new EnrollmentRepository()).toBeDefined();
  });
});
